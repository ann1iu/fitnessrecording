name: fitnessrecording
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^3.0.0
  path_provider: ^2.1.5
  drift: ^2.28.1
  drift_flutter: ^0.2.5
  sqflite: ^2.4.2
  path: ^1.9.1
  logging: ^1.3.0
  intl: ^0.20.2
  google_fonts: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  build_runner: ^2.7.0
  drift_dev: ^2.28.1

flutter:
  uses-material-design: true
  assets:
    - assets/